# Node modules (any depth — not just root)
node_modules/

# Secrets — never commit
.env
.env.local
.env.*.local
*.key

# OS files
.DS_Store

# Build / compilation output (any depth)
dist/
bundle/

# Hardhat build artifacts & compilation cache (any depth)
artifacts/
cache/

# Typechain output
types/

# Hardhat coverage reports
coverage/
